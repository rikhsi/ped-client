import { ModalOptions } from 'ng-zorro-antd/modal';

/**
 * Стандартные настройки модального окна (ng-zorro-antd).
 *
 * Используется для унификации внешнего вида и поведения модальных окон в приложении.
 */
export const MODAL_OPTIONS: ModalOptions = {
  /** Ширина модального окна. 'auto' — подстраивается под содержимое */
  nzWidth: 'auto',

  /** Футер модального окна. null — без футера */
  nzFooter: null,

  /** Заголовок модального окна */
  nzTitle: '',

  /** Флаг, разрешающий закрытие модального окна через крестик */
  nzClosable: false,

  /** Иконка для кнопки закрытия. Пустая строка — без иконки */
  nzCloseIcon: '',

  /** Центрирование модального окна по вертикали */
  nzCentered: true,

  /** Фокусировка на элементе внутри модального окна при открытии. null — авто по умолчанию */
  nzAutofocus: null,
};
