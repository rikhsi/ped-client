/**
 * Ключи для локального хранилища (localStorage).
 * Используются для сохранения настроек пользователя и данных сессии.
 */
export enum LocalStorageItem {
  /** Выбранный язык интерфейса */
  LANG = 'lang',

  /** Токен доступа для авторизации */
  ACCESS_TOKEN = 'access_token',
}
